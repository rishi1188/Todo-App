/**
 * tasks/enums/priority.enum.ts
 *
 * Mirrors the Priority enum on the mobile app so the two sides agree
 * on valid values ('low' | 'medium' | 'high').
 */

export enum Priority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}
